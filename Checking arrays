//сравнение двух массивов между собой
function areArraysSame (a1, a2){
  if(a1.length!= a2.length ){
    return false;
  }
  for (var i=0; i < a1.length; i++){
    if (a1[i]!=a2[i]){
      return false;
    }
    else return true;
  }
}
areArraysSame([1, 2, 3], [1, 2 ,3]);
